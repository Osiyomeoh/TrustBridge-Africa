# Pinata IPFS Configuration for Frontend
# Copy these variables to your .env.local file

# Pinata Configuration
VITE_PINATA_JWT=your_pinata_jwt_token_here
VITE_PINATA_GATEWAY_URL=your_gateway_domain.mypinata.cloud

# Server URL for presigned URLs
VITE_SERVER_URL=http://localhost:4001/api

# Instructions:
# 1. Go to https://pinata.cloud and sign up for an account
# 2. Go to API Keys section in your dashboard
# 3. Create a new API key with these permissions:
#    - pinFileToIPFS (Upload files)
#    - pinByHash (Pin existing files)
#    - unpin (Remove files)
#    - pinList (List files)
# 4. Copy the API Key and Secret Key
# 5. Go to Gateways section to get your gateway URL
# 6. For VITE_PINATA_JWT, you can use the same API key as PINATA_API_KEY
# 7. Replace the values above with your actual credentials
# 8. Add these lines to your .env.local file
